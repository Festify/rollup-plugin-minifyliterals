function str(){
  let hello = 'hello'
  let world = 'world'
  let fill = 'ab'

  return '<a class="' + fill + '" id="' + fill + '" name="' + fill + '"><span class="' + fill + '">' + hello + '</span> <span>' + world + '</span></a>'
}
